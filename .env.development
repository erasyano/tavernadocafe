# Desenvolvimento Local
cp .env.example .env

# Configure estas variáveis no arquivo .env:

# 1. Discord Developer Portal (https://discord.com/developers/applications)
#    - Crie uma nova Application
#    - Copie Client ID e Client Secret
#    - Adicione redirect: http://localhost:3000/api/auth/callback

# 2. Gere JWT_SECRET:
#    node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"

# 3. URLs para desenvolvimento local (padrão):
DISCORD_REDIRECT_URI=http://localhost:3000/api/auth/callback
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# ============================================================
# PRODUÇÃO (SquareCloud)
# ============================================================

# Quando for fazer deploy:
# 1. Atualize DISCORD_REDIRECT_URI com sua URL de produção
# 2. Atualize NEXT_PUBLIC_BASE_URL com sua URL de produção
# 3. No Discord Developer Portal, adicione a URL de produção nas Redirects
# 4. Configure as mesmas variáveis no painel da SquareCloud

# Exemplo produção:
# DISCORD_REDIRECT_URI=https://tavernadocafe.squareweb.app/api/auth/callback
# NEXT_PUBLIC_BASE_URL=https://tavernadocafe.squareweb.app
